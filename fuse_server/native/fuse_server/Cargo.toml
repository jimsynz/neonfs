[package]
name = "fuse_server"
version = "0.3.2"
authors = []
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
rustler = { version = "0.37.2", features = ["nif_version_2_16"] }
libc = "0.2"
