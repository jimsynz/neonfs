defmodule NeonFS.WebDAV do
  @moduledoc """
  WebDAV interface for NeonFS volumes.

  Maps WebDAV collections to NeonFS volumes and resources to file paths,
  communicating with core nodes via `NeonFS.Client.Router`.
  """
end
