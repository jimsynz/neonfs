# Advisories temporarily ignored by `mix deps.audit`.
#
# Each line is a `GHSA-...` id. Blank lines and lines starting with
# `#` are skipped (mix_audit/lib/cli/audit.ex). Re-evaluate every
# entry whenever the upstream package publishes a new release.
#
# -- GHSA-g2wm-735q-3f56 (cowlib < 2.16.2) --
# "Cookie Request Header Injection via Unvalidated Encoder in
#  cow_cookie:cookie/1." Severity: low. cowlib has not yet
# released a patched version (advisory `First patched versions`
# is empty), so we have nothing to bump to. NeonFS does not
# encode untrusted cookies via cowlib — the cluster's HTTP
# surface (`Bandit` + `Plug`) consumes incoming request headers
# rather than constructing outbound cookie headers from
# user-controlled strings. Tracked for removal under #1027.
GHSA-g2wm-735q-3f56
